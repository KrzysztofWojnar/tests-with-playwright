{
  "name": "unnamed-testing-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "e2e": "npx playwright test",
    "e2e:chromium": "npx playwright test --project=chromium",
    "e2e:firefox": "npx playwright test --project=firefox",
    "e2e:api": "npx playwright test --project=api-tests",
    "e2e:trace-all": "cross-env PW_TRACE=on npx playwright test --project=chromium",
    "build": "tsc",
    "test": "cross-env npm run gherkin:generate && npm run build && codeceptjs run --config dist/codecept.conf.js",
    "test:headed": "cross-env npm run gherkin:generate && HEADLESS=false npm run build && codeceptjs run --config dist/codecept.conf.js",
    "test:debug": "cross-env npm run gherkin:generate && npm run build && codeceptjs run --config dist/codecept.conf.js --debug",
    "test:watch": "cross-env npm run gherkin:generate && npm run build && codeceptjs run --config dist/codecept.conf.js --watch",
    "gherkin:init": "codeceptjs gherkin:init",
    "gherkin:generate": "codeceptjs gherkin:build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@codeceptjs/configure": "^1.0.6",
    "@playwright/test": "^1.58.0",
    "@types/node": "^25.1.0",
    "codeceptjs": "^3.7.6",
    "cross-env": "^10.1.0",
    "dotenv": "^17.2.3",
    "eslint": "^9.39.2",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-prettier": "^5.5.5",
    "globals": "^17.3.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.54.0",
    "zod": "^4.3.6"
  }
}
